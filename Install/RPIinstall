# get the basic RPi system installed and running.  I create my own account to
# access the machine and start things up.  When the basic system is up,
# Download the code as user pi and use the build script to install all the
# needed software.
#
# the software itself will check for new movies and download them in chunks
# when you want them to.  
#
# Bill Cheswick
# ches@cheswick.com

add user ches, and add ches to sudoers
ssh access to host

raspi-conf:
various internationalizations.  Activate ssh.  Auto boot to acct pi later.

add /etc/wpa*/ches.conf  for my local WiFi
connect ethernet and

apt-get update
apt-get install jove
apt-get upgrade
